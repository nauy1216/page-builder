/**
 *  1、这个文件是动态生成的， 执行npm run build:file即可。
 *  2、请不要手动写这里的代码， 将会被覆盖。
 */
import WuiSwipper from "./packages/swipper/index.vue";
import WuiSwipperConfig from "./packages/swipper/config";

export default {
  WuiSwipper
};

export const componentsConfig = {
  WuiSwipper: WuiSwipperConfig
};
